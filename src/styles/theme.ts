const color = {
  primary_light: '#fff93b',
  primary_normal: '#7b5339',
  secondary_light: '#beaa9e',
};

const theme = {
  color,
};

export type Theme = typeof theme;

export default theme;
